const Display = ({ counter }) => {
    return (
      <div>{counter}</div>
    )
  }

export default Display

//const Display = ({ counter }) => <div>{counter}</div> on peut mettre ça comme ça (tres raccourci)
